/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  images: {
    domains: ['scontent.cdninstagram.com'],
    // unoptimized: true
  },
}

module.exports = nextConfig
